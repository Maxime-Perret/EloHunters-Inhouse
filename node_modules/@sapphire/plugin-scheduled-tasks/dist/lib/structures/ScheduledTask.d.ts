import { Piece } from '@sapphire/pieces';
import type { Awaitable } from '@sapphire/utilities';
export declare abstract class ScheduledTask extends Piece {
    readonly interval: number | null;
    readonly cron: string | null;
    readonly bullJobOptions?: unknown;
    constructor(context: Piece.Context, options: ScheduledTaskOptions);
    abstract run(payload: unknown): Awaitable<unknown>;
}
export interface ScheduledTaskOptions extends Piece.Options {
    interval?: number | null;
    cron?: string | null;
}
export declare namespace ScheduledTask {
    type Options = ScheduledTaskOptions;
    type Context = Piece.Context;
}
//# sourceMappingURL=ScheduledTask.d.ts.map